[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "pyne-runtime"
version = "0.3.0"
description = "Pine-style Python indicator runtime for OHLCV data."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [
  { name = "CandleScope contributors" }
]
keywords = ["technical-analysis", "indicators", "ohlcv", "pine-script", "trading"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
  "numpy>=1.26",
]

[tool.pyne-runtime]
published-version = "0.3.0"

[project.urls]
Homepage = "https://github.com/helenananaa/pyne-runtime"
Repository = "https://github.com/helenananaa/pyne-runtime"
Documentation = "https://github.com/helenananaa/pyne-runtime/tree/main/docs"
Issues = "https://github.com/helenananaa/pyne-runtime/issues"

[project.optional-dependencies]
pandas = ["pandas>=2"]
plot = ["matplotlib>=3"]
dev = [
  "build>=1",
  "hatchling>=1.25",
  "pytest>=7",
  "ruff==0.15.13",
  "twine>=7",
]
all = [
  "pandas>=2",
  "matplotlib>=3",
]

[project.scripts]
pyne = "pyne_runtime.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pyne_runtime"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
"examples/*.py" = ["F821"]
