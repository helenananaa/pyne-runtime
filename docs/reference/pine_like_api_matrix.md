# Pine-Like API Matrix

Pyne is a Python runtime with Pine-like semantics. It does not run TradingView
Pine source code directly. This matrix tracks the supported mental model, the
Pyne API surface, and the test/doc evidence behind each claim.

Current capability status, explicit limits, and validation evidence are tracked
in [Current Project Status](current_status.md). Future priorities are maintained
in the [Python package long-term direction](../development/python_package_long_term_plan_zh.md).

Additional whole-script Pine v6 evidence now covers EMA/MACD seed and gap behavior,
RSI flat/rising/falling/gap cases, RSI-to-EMA composition and batch TSI. These
captures are exercised by `tests/test_ta_external_boundaries.py` separately from
the 10 imported TA fixture totals below. See the
[TA boundary acceptance report](../development/ta_boundary_acceptance_zh.md).

Further pytest-based evidence covers native Supertrend/VWMA, custom-volume SMA
ratios, and market-order versus pending-order OCA outcomes with positive and
negative controls. See [trend/volume/OCA acceptance](../development/trend_volume_oca_acceptance_zh.md).
These captures also remain separate from the imported fixture counts below.

The additional [rolling statistics capture](../development/rolling_statistics_acceptance_zh.md)
checks SMA/stdev/variance/BB missing-value windows. Two high-offset Pine dispersion
columns are reference-only numerical differences; ordinary-range columns are
parity-tested, and Pyne's stable results are checked against centered arithmetic.

| Feature | Pyne API | Status | Known Differences | Tests | Docs |
| --- | --- | --- | --- | --- | --- |
| OHLCV sources | `open`, `high`, `low`, `close`, `volume` | Supported | Values are `PyneSeries`, not Pine native series | `tests/test_series.py` | `docs/concepts/data_model.md` |
| History references | `close[1]`, `high[2]` | Supported | Negative indexes are rejected and `pyne validate` reports migration hints for common forward-history spellings; Python positional indexing uses `.values`/NumPy internally | `tests/test_series.py`, `tests/test_api.py` | `docs/concepts/series_semantics.md`, `docs/tutorials/pine_to_pyne_cookbook.md` |
| Series arithmetic | `high + low`, `(close - open) / open` | Supported | Python operator precedence applies | `tests/test_series.py` | `docs/concepts/series_semantics.md` |
| Series comparisons | `close > open` | Supported | Use `&`, `|`, `~` for boolean composition; `pyne validate` reports migration hints for Python `and` / `or` / `not` on series conditions | `tests/test_series.py`, `tests/test_api.py` | `docs/concepts/expression_helpers.md`, `docs/tutorials/pine_to_pyne_cookbook.md` |
| Series conditionals | `when(cond, a, b)`, `switch(...)` | Supported | Python ternary and `if` cannot branch on a series; `pyne validate` reports migration hints for common series `if` and ternary mistakes | `tests/test_expression_helpers.py`, `tests/test_api.py` | `docs/concepts/expression_helpers.md`, `docs/tutorials/pine_to_pyne_cookbook.md` |
| Missing values | `na`, `na(x)`, `nz(x, fallback)`, `fixnan(x)` | Supported | `na` is a callable sentinel object in Python; `fixnan()` carries the latest non-`na` value forward while preserving leading missing values | `tests/test_na_semantics.py` | `docs/concepts/na_semantics.md` |
| Bar clock | `bar_index`, `last_bar_index`, `time`, `time_close`; incremental `ctx.bar_index`, `bar.bar_index` | Supported | Batch runtime exposes full-series values; inferred final `time_close` is `na` when not supplied; incremental callbacks expose scalar current-bar values | `tests/test_barstate.py`, `tests/test_incremental.py` | `docs/concepts/bar_execution_model.md` |
| Bar flags | `barstate.isfirst`, `barstate.islast`, `barstate.isconfirmed`; incremental `ctx.barstate.*` | Supported | Incremental preview state is runtime-event based; exact market-session open/closed inference is host responsibility | `tests/test_barstate.py`, `tests/test_incremental.py` | `docs/concepts/bar_execution_model.md` |
| Runtime metadata | `syminfo.mintick`, `syminfo.tickerid`, `timeframe.period`, `timeframe.multiplier`, `timeframe.in_seconds()`, `timeframe.from_seconds()`, `timeframe.change()`, `dayofweek`, `session.ismarket` | Supported | Metadata is supplied by the host or `PyneSettings`; timeframe calendar boundaries use host `syminfo.timezone` or UTC; batch `session.*` flags are series, while incremental `ctx.session.*` flags are current-bar scalars | `tests/test_metadata_runtime.py`, `tests/test_time_runtime.py`, `tests/test_incremental.py` | `docs/api/settings.md`, `docs/api/time.md` |
| Input parameters | callable `input(...)`; `input.int`, `input.float`, `input.bool`, `input.string`, `input.color`, `input.source`, `input.timeframe`, `input.symbol`, `input.session`, `input.time`, `input.text_area`, `input.price`, `input.enum`; schema fields `id`, `current`, `group`, `inline`, `tooltip`, `options`, `minval`, `maxval`, `step`, `confirm`, `display`, `active`; result `paramSchemaVersion` | Partial | Host renders the UI; repeated titles keep their display title but receive stable suffixed `key` / `id` values after the first declaration; numeric schema keeps legacy `min`/`max` aliases; input timeframe/symbol/session values are script params and do not override runtime metadata unless the host also passes metadata | `tests/test_input_runtime.py`, `tests/test_examples.py`, `tests/test_schema.py` | `docs/api/input.md`, `docs/reference/output_schema.md` |
| Runtime state | `var()`, `pyne.var()`, `set_each()`; incremental `ctx.state()`, `ctx.state("x")[1]`, `ctx.varip()`, `state()`, `varip()`, `pyne.varip()` | Supported | Full Pine `:=` recursive assignment is represented through explicit state cells; incremental `ctx.state()` keeps committed snapshot history for bars-back reads; incremental `ctx.varip()`/`varip()` persists within one realtime preview bar and resets before confirmed callbacks | `tests/test_state_runtime.py`, `tests/test_incremental.py` | `docs/concepts/state_semantics.md`, `docs/concepts/bar_execution_model.md`, `docs/concepts/incremental_runtime.md` |
| Collections | `array.new_float(2, 1.0)`, `array.push(a, close[1])`, `array.first(a)`, `array.last(a)`, `array.get(a, -1)`, `array.sort(a, order.descending)`, `array.sort_indices(a)`, `array.snapshot(a)`, `a.avg()`; `map.put(m, "fast", 10)`, `map.put_all(target, source)`, `map.get(m, "fast")`, `map.snapshot(m)`, `map.keys(m)`; `matrix.new_float(2, 2, 0.0)`, `matrix.new_bool(1, 1, True)`, `matrix.new_string(1, 1, "fast")`, `matrix.new_color(1, 1, color.red)`, `matrix.get(m, 0, 1)`, `matrix.snapshot(m)`, `matrix.mult(a, b)` | Supported | `array.from(...)` is exposed as `array.from_values(...)` / `array.from_list(...)` because `from` is a Python keyword; `pyne validate` reports a migration hint for the keyword conflict; arrays/maps/matrices are Python objects, not Pine handles; array sizes and matrix dimensions must be non-negative; empty `array.pop()` / `array.shift()` calls are explicit runtime errors; `array.sort()` and `array.sort_indices()` accept Pine-like `order.ascending` / `order.descending`; `snapshot()` recursively freezes nested collection values; incremental `ctx.state()` history snapshots preserve prior confirmed array/map/matrix values, including nested collections, after current mutations; script-created collections enforce `PyneSettings` array/map/matrix size and nested-depth limits; callable/module and recursive collection values are rejected as unsupported stored state | `tests/test_collections_runtime.py`, `tests/test_incremental.py`, `tests/test_api.py` | `docs/api/collections.md`, `docs/concepts/incremental_runtime.md`, `docs/tutorials/pine_to_pyne_cookbook.md` |
| Strings | `str.tostring(close, "#.##")`, `str.tonumber("42.5")`, `str.match("NASDAQ:AAPL", "[A-Z]+$")`, `str.split("ema,sma", ",")`, `str.replace_all(text, "a", "b")` | Supported | Pyne injects a callable `str` namespace, so `str(value)` still works; batch `str.tostring(series)` resolves to the latest non-`na` value rather than a native Pine series string; `str.match()` uses Python regular expressions and returns `None` when no match is found | `tests/test_string_runtime.py` | `docs/api/string.md` |
| Tickers | `ticker.new("NASDAQ", "AAPL")`, `ticker.modify(symbol, session=ticker.session_extended)`, `ticker.heikinashi(syminfo.tickerid)`, `ticker.standard(symbol)` | Supported | Ticker ids are stable strings; host data providers decide how to resolve modifier query parameters | `tests/test_ticker_runtime.py` | `docs/api/ticker.md` |
| Time helpers | `time[1]`, `time("60")`, `time("", "0930-1600:23456", "America/New_York")`, `time("1D", bars_back=2, timeframe_bars_back=1)`, `time.year()`, `time.hour(time, "+08:00")`, `time.timestamp(2024, 1, 2)`, `time.format(...)`, `dayofweek == dayofweek.monday` | Supported | `time` and `dayofweek` remain series-like objects; timestamps are interpreted as seconds unless they look like milliseconds; current-timeframe calls preserve host bar opens, while derived higher-timeframe opens use calendar/24x7 boundaries and do not reconstruct exchange-session-specific bar opens from a ticker string | `tests/test_time_runtime.py` | `docs/api/time.md` |
| Colors | `color.rgb(255, 128, 0)`, `color.new(color.green, 80)`, `color.r(color.blue)`, `color.t(series_color)`, `color.from_gradient(rsi, 0, 100, color.red, color.green)` | Supported | Colors serialize as CSS hex or `rgba(...)` strings; channel accessors support scalar and series colors; host renderers own final display semantics | `tests/test_color_runtime.py` | `docs/api/color.md` |
| Math helpers | `math.sum(close, 20)`, `math.max(high, close, open)`, `math.min(low, close, open)`, `math.avg(open, high, low, close)`, `math.round(close, precision=2)`, `math.trunc(close)`, `math.fixnan(close)`, `math.round_to_mintick(close)`, `math.random(0, 1, seed=42)` | Supported | `math.fixnan()` carries the latest non-`na` value forward while preserving leading missing values; `math.random()` is runtime-local randomness rather than TradingView engine state; `round_to_mintick()` uses `PyneSettings.syminfo.mintick` | `tests/test_math_runtime.py` | `docs/api/math.md` |
| Core TA | `ta.sma`, `ta.ema`, `ta.rma`, `ta.rsi`, `ta.macd`, `ta.bb`, `ta.atr` | Supported | All 10 committed TA capture fixtures are TradingView parity-gated with 0 diff across 104 plots and 1,353 checked points; numerical parity remains best-effort outside covered fixtures; MACD histogram follows Pine's `macd - signal` tuple value; `ta.bb` returns `middle, upper, lower` | `tests/test_ta_runtime.py`, `tests/test_golden_ta.py`, `tests/test_ta_capture_*.py` | `docs/api/ta.md`, `docs/development/non_strategy_capture_plan_zh.md` |
| Expanded TA | `ta.alma`, `ta.hma`, `ta.swma`, `ta.dmi`, `ta.sar`, `ta.stoch`, `ta.cci`, `ta.mfi`, `ta.vwma`, `ta.vwap`, `vwap`, `ta.pivot_point_levels`, `ta.supertrend`, `ta.highestbars`, `ta.lowestbars`, `ta.barssince`, `ta.valuewhen`, percentiles | Supported | The current TradingView capture set covers trend switches, oscillator/statistics edges, tuple outputs, and warm-up boundaries; `ta.vwap` and all six pivot-point formula families have deterministic semantic tests but no imported TradingView capture yet; context-aware helper signatures use Pine-like argument order; inputs and functions outside committed captures remain best-effort | `tests/test_ta_runtime.py`, `tests/test_golden_ta.py`, `tests/test_ta_capture_*.py` | `docs/api/ta.md`, `docs/development/non_strategy_capture_plan_zh.md` |
| Incremental TA | `ctx.ta.adx/alma/atr/barssince/bb/boll/cci/change/cross/crossover/crossunder/cum/dev/dmi/ema/highest/highestbars/hma/lowest/lowestbars/macd/mfi/pivot_point_levels/pivothigh/pivotlow/rma/rsi/sar/sma/stdev/stoch/supertrend/swma/tr/valuewhen/variance/vwap/vwma/wma` | Partial | These 39 scalar helpers have explicit state and resource bounds, batch parity, and portable-restore coverage; other batch `ta.*` members fail early when statically visible instead of being implied by namespace similarity | `tests/test_incremental.py`, `tests/test_incremental_ta_demand.py`, `tests/test_incremental_ta_expansion.py`, `tests/test_incremental_ta_phase2.py`, `tests/test_runtime_capabilities.py` | `docs/concepts/incremental_runtime.md`, `docs/api/capabilities.md` |
| Pinned external library | `pine_library("TradingView/ta/10").atr2/cagr/changePercent/ema2/highestSince/lowestSince/requestUpAndDownVolume/requestVolumeDelta/rma2` | Partial | The adapter is batch-only; only the pinned version and nine allowlisted members are implemented; only volume members require authoritative host lower-timeframe data; the three dynamic-length additions have deterministic semantic tests but no imported TradingView capture yet; unknown identifiers fail closed | `tests/test_render_ir_v2.py`, `tests/test_runtime_capabilities.py` | `docs/api/pine_libraries.md`, `docs/api/capabilities.md` |
| Plot output | `plot(close, display=display.all, format=format.price)`, incremental `ctx.plot("Close", bar.close, style=plot.style_columns)`, `hline`, `fill`, `plotshape(close > open, style=shape.triangleup, location=location.abovebar)`, `plotchar(close > open, char="*", location=location.abovebar)`, `plotarrow(close - open, colorup=color.green, colordown=color.red)`, `marker(shape=shape.triangleup, location=location.abovebar, size=size.small)`, incremental `ctx.marker(..., shape=shape.square, position=location.abovebar, size=size.large)`, `bgcolor`, `barcolor`; indicator metadata such as `scale=scale.right` | Supported | Output is a host-renderable JSON schema; incremental committed plot/marker output follows batch renderer fields for covered line style, histogram columns, marker shape/location/size, and indicator overlay pane defaults | `tests/test_plot_runtime.py`, `tests/test_incremental.py` | `docs/api/plot.md`, `docs/concepts/incremental_runtime.md` |
| Drawing objects | `chart.point.new/now/from_index/from_time/copy`, line/label/box point overloads and setters, `linefill.new`, `polyline.new`, `line.all`, `box.all`, `line.new(..., xloc=xloc.bar_time)`, `label.new(..., yloc=yloc.abovebar)`, `box.new`, `table.new(position.top_right, ...)`, `table.merge_cells(...)`, setters, `delete()`, enum namespaces; incremental callback object mutations | Supported | Batch output is a final snapshot; incremental Render IR v2 includes the current line/label/box/table/linefill/polyline snapshot plus time-filtered create/update/delete `object_events`; preview events and merged cells run on cloned state and do not persist | `tests/test_plot_runtime.py`, `tests/test_incremental.py`, `tests/test_incremental_render_ir_v2.py` | `docs/concepts/drawing_objects.md`, `docs/api/plot.md`, `docs/concepts/incremental_runtime.md` |
| Alerts/signals | `alertcondition`, `emit_signal` | Supported | Emits structured events; does not register TradingView alerts | `tests/test_plot_runtime.py` | `docs/api/plot.md` |
| Multi-context data | `request.security(symbol, timeframe, lambda ctx: ctx.ta.sma(ctx.close, 20), gaps=barmerge.gaps_on, lookahead=barmerge.lookahead_off, ignore_invalid_symbol=True)`; `request.security_lower_tf(symbol, timeframe, lambda ctx: ctx.close, ignore_invalid_symbol=True)`; tuple thunks such as `lambda ctx: (ctx.open, ctx.close)` | Partial | Requires host provider; direct Python expression capture is impossible, so computed expressions use callable thunks; request behavior is TradingView parity-gated across 21/21 captured fixtures with 0 diff, covering HTF, LTF, same-timeframe, gaps/lookahead, requested-context history/time/time_close/session/timezone/metadata, invalid symbol/timeframe ignore paths, lower-timeframe tuple/grouping/array helpers, and requested OHLCV replay; requested-context history captures may need the prior provider bar before the exported chart window; provider bars are sorted by `time` before alignment/grouping; provider cache diagnostics distinguish legal empty `[]` results (`status="ok"`, `bars=0`, cached and reusable across request shapes) from `PyneInvalidSymbolError` ignored by `ignore_invalid_symbol=True` (`status="ignoredInvalidSymbol"`, not cached); invalid provider return types remain contract failures even when invalid symbols are ignored; lower-timeframe requests return grouped Python/Pyne objects rather than Pine native arrays; provider contract is exposed in `pn.schema()["requestProvider"]`, including `supportedApis`, structured `errorCategories`, `errorDetail.requestProviderRequest`, request `resultShape`, and `meta.requestDiagnostics` for host diagnostics; `pn.run(..., data_provider=...)`, `PyneSettings(data_provider=...)`, and `execute_pyne_script(..., data_provider=...)` are typed against the public `DataProvider` protocol | `tests/test_request_security.py`, `tests/test_golden_request_security.py`, `tests/test_schema.py`, `tests/test_api.py`, `tests/test_request_capture_*.py` | `docs/api/request.md`, `docs/reference/schema_migrations.md` |
| Strategy events | `strategy(...)`, `strategy.entry_when`, `strategy.order_when`, `strategy.cancel`, `strategy.cancel_all`, `strategy.close(qty=..., qty_percent=...)`, `strategy.close_all`, `strategy.exit(qty=..., qty_percent=...)`, `strategy.oca.cancel`, `strategy.oca.reduce`, `slippage`, `commission_type`, `commission_value`, `backtest_fill_limits_assumption`, `same_bar_fill_priority`, `intrabar_path`, `margin_long`, `margin_short` | Partial | Deterministic replay layer only; no full broker simulation; market round-trip, pending limit/stop entry, bracket exit, commission/slippage, percent commission, cash-per-contract, partial-close allocation, reversal, pyramiding, OCA/risk-lock, global drawdown lock, max position size, limit verification, partial exit, same-bar priority, intrabar path, mirrored short-side, margin admission, lower-level `strategy.order`, and cancel/cancel_all fixtures carry imported TradingView captures; all 27 strategy pine-equivalent cases are TradingView parity-gated with 0 diff across 168 plots and 497 checked points; same-bar stop/limit ambiguity is resolved by explicit priority or high/low path policy; OCA cancel/reduce and long/short/reversal/pending-fill/same-bar/limit-verification/bracket/risk-margin/entry-sizing/reversal-lot/OCA/cancel-risk/mixed lifecycle cost-model behavior are covered by golden fixtures; incremental committed-bar strategy covers matching slippage, commission, margin gating, market-like, pending, close, immediate exit fill behavior, and one-shot persistent exit orders that fill on later committed bars; incremental preview strategy reports are temporary overlays and preview market or pending orders do not persist into `snapshot_result()` or later confirmed fills | `tests/test_strategy_runtime.py`, `tests/test_golden_strategy.py`, `tests/test_incremental.py`, `tests/test_strategy_capture_*.py` | `docs/api/strategy.md`, `docs/concepts/bar_execution_model.md`, `docs/concepts/incremental_runtime.md`, `docs/development/tradingview_capture_next_phase_zh.md` |
| Strategy reporting | `strategy.equity`, `strategy.netprofit`, `strategy.openprofit`, `strategy.grossprofit`, `strategy.grossloss`; `strategy.closedtrades.profit(0)`, `strategy.closedtrades.profit_percent(0)`, `strategy.closedtrades.commission(0)`, `strategy.closedtrades.entry_id(0)`, `strategy.closedtrades.entry_comment(0)`, `strategy.closedtrades.exit_comment(0)`, `strategy.closedtrades.entry_bar_index(0)`, `strategy.closedtrades.exit_bar_index(0)`, `strategy.opentrades.entry_price(-1)`; incremental `ctx.strategy.closedtrades.profit(0)`, `ctx.strategy.opentrades.size(-1)`; output `summary`, `closedtrades`, `opentrades`, `lifecycle` | Partial | Trade ledgers track entry lots, close/exit can target entry lots, `profit_percent()` reports profit over absolute entry notional, non-empty entry/exit comments are exposed through trade accessors and public trade reports, closed-trade commission includes proportional entry and exit/close commission, partial-close trade-profit reporting follows the captured TradingView accessor behavior, negative trade indexes count from the latest ledger tail, missing numeric fields return `na`, missing string fields return `""`, and lifecycle reports submission/pending/fill/cancel/rejection phases plus requested/filled quantity details; bar-index trade accessors are script-facing and do not add private fields to the public trade report; incremental callbacks expose scalar closed/open trade-ledger accessors for committed bars; position replay is still deterministic net-position based rather than a full broker emulator | `tests/test_strategy_runtime.py`, `tests/test_golden_strategy.py`, `tests/test_incremental.py` | `docs/api/strategy.md`, `docs/concepts/bar_execution_model.md` |
| Strategy risk | `strategy.risk.allow_entry_in(strategy.direction.long)`, `strategy.risk.max_drawdown(20, strategy.percent_of_equity)`, `strategy.risk.max_intraday_loss(5, strategy.cash)`, `strategy.risk.max_position_size(10)`, `strategy.risk.max_intraday_filled_orders(3)`; incremental `ctx.strategy.risk.*` equivalents | Partial | Risk rules are deterministic replay logic; drawdown locks globally, intraday limits reset at `session.isfirstbar`, max position size caps entries, and none model broker liquidation; risk lock, pending-order risk gating, close/exit recovery under lock, and intraday reset behavior are covered by golden fixtures; incremental committed-bar strategy now covers core direction, position-size, drawdown, intraday loss, filled-order risk, cost-accounting, and margin-gating parity | `tests/test_strategy_runtime.py`, `tests/test_golden_strategy.py`, `tests/test_incremental.py` | `docs/api/strategy.md`, `docs/concepts/bar_execution_model.md` |
| Public package API | `pn.run`, `pn.PyneRuntime`, `pn.PyneSettings`, `pn.na`, `pn.nz`, `pn.fixnan` | Supported | Pre-1.0 minor versions may still add APIs | `tests/test_api.py` | `docs/api/public_api.md` |
| Capability and trace contracts | `pn.runtime_capabilities()`, `pn.inspect_script()`, `pn.validate(..., runtime_mode=...)`, `trace.emit(...)`, `ctx.trace.emit(...)` | Supported | Inspection is static and non-executing; trace v2 is opt-in, bounded, redacted diagnostic/timing evidence rather than a durable security log; hard-killed processes cannot return in-memory trace state | `tests/test_script_inspection.py`, `tests/test_runtime_capabilities.py`, `tests/test_execution_trace.py` | `docs/api/capabilities.md`, `docs/concepts/execution_trace.md` |
| CLI | `pyne run`, `pyne schema`, `pyne validate`, `pyne inspect` | Supported | CLI executes Pyne Python scripts, not Pine source files; inspect performs static AST preflight without execution | `tests/test_cli.py`, `tests/test_cli_contracts.py`, `tests/test_script_inspection.py` | `docs/reference/cli.md` |

## Planned Gaps

- Expand beyond the declared 39 incremental TA helpers and nine pinned-library
  members only for concrete converted-script demand, with parity fixtures for
  each promoted surface.
- The standalone `0.3.0rc2` source candidate passes local build and isolated
  installed-wheel gates; product-specific adapter acceptance remains outside
  this repository, while publishing, tag, and remote matrix CI are separate
  release decisions.
- Keep replay-v1 and typed-state-v2 portable snapshots bounded and fail closed;
  distributed session ownership, provider lifecycle, and storage orchestration
  remain host work.
